slam_toolbox:
  ros__parameters:
    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS params -- the Stretch sim publishes odom->base_link, /scan is the
    # RTX lidar on the "laser" frame. slam_toolbox supplies map->odom.
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan
    use_map_saver: true
    mode: mapping
    
    # Load the previously serialized apartment map and localize against it
    # instead of building a new one. Switch back to mode: mapping (and remove
    # map_file_name) to map again. Re-serialize with the /slam_toolbox/serialize_map
    # service after any further mapping.
    # mode: localization
    # map_file_name: /mnt/dev-tools/isaacsim-template/nav2_ws/maps/apartment
    # The robot spawns at world (-1.5, 0, yaw 0) (apartment.py), and odom==world
    # in this sim (ground-truth odometry). The saved map frame is rotated ~155deg
    # from world (slam_toolbox anchored its origin at the first scan), so the
    # spawn expressed in MAP coordinates is the value below -- NOT [0,0,0].
    # If the robot ever spawns elsewhere, recompute or use RViz "2D Pose Estimate".
    # map_start_pose: [0.0, 0.0, 0.0]

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02   # 50 Hz map->odom
    map_update_interval: 1.0
    resolution: 0.05
    min_laser_range: 0.15
    max_laser_range: 12.0
    minimum_time_interval: 0.2
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true

    # General Mapping params
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.3
    minimum_travel_heading: 0.3
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 12.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
